# Node.js 内存限制配置（生产环境建议 4096MB）
NODE_OPTIONS=--max-old-space-size=4096

# Next.js 遥测（设为 1 禁用）
NEXT_TELEMETRY_DISABLED=1

# 可选：自定义Exchange Rate API（如果需要实时汇率）
# EXCHANGE_RATE_API_URL=https://api.exchangerate-api.com/v6/latest/CNY